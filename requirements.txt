# AUDIT-D2: everything below carries an exact pin. Only `redis` was pinned before, so any
# rebuild silently picked up whatever was current on PyPI that day, major versions included.
# The versions here are the ones the running worker was already on when they were taken, so
# adopting them is behaviourally a no-op.
redis==8.1.0
minio==7.2.20
# PaddlePaddle has no Linux ARM64 wheel. RapidOCR converts the same PP-OCR model
# family to ONNX and uses the already-pinned cross-platform ONNX Runtime package.
rapidocr==3.9.2; sys_platform == "linux" and (platform_machine == "aarch64" or platform_machine == "arm64")
shapely==2.1.2
paddleocr==3.7.0; sys_platform != "linux" or (platform_machine != "aarch64" and platform_machine != "arm64")
paddlepaddle==3.3.1; sys_platform != "linux" or (platform_machine != "aarch64" and platform_machine != "arm64")
numpy==2.5.3
requests==2.34.2
# RapidOCR declares the non-headless opencv-python distribution; use one OpenCV
# flavor per architecture so pip does not install two packages that share the cv2 namespace.
opencv-python==5.0.0.93; sys_platform == "linux" and (platform_machine == "aarch64" or platform_machine == "arm64")
opencv-python-headless==5.0.0.93; sys_platform != "linux" or (platform_machine != "aarch64" and platform_machine != "arm64")
psutil==7.2.2
onnxruntime==1.29.0
rq==2.12.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
tenacity==9.1.4
httpx==0.28.1
pytest==9.1.1
pytest-cov==7.1.0
pytest-mock==3.15.1
ruff==0.16.5
pyright==1.1.411
pydantic==2.13.5
# Hyphenation dictionaries for the renderer's line breaking (D7). Pure Python, no native deps.
pyphen==0.18.1
